# Smart Health Assist v2.0 - Environment Configuration File

# =================== Ollama Settings ===================
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=mistral

# =================== Embedding Model Settings ===================
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# =================== ChromaDB Settings ===================
CHROMA_DB_PATH=./data/chroma_db
COLLECTION_NAME=health_knowledge

# =================== Server Settings ===================
SERVER_HOST='localhost'      #'172.18.3.20'
SERVER_PORT=8000

# =================== Device Settings ===================
EMBEDDING_DEVICE_POPULATE=auto
EMBEDDING_DEVICE_RUNTIME=cpu

# =================== CORS Settings ===================
# IMPORTANT: Use JSON array format with quotes around each item
ALLOWED_ORIGINS=["http://localhost:3000", "http://localhost:8000", " https://rhett-subnasal-wholly.ngrok-free.dev"]
ALLOWED_METHODS=["GET", "POST", "OPTIONS"]
ALLOWED_HEADERS=["Content-Type", "Authorization"]

# =================== Rate Limiting Settings ===================
RATE_LIMIT_PER_MINUTE=30

# =================== ChromaDB Batch Settings ===================
CHROMA_BATCH_SIZE=5000

# =================== Logging Settings ===================
LOG_LEVEL=INFO

# =================== Application Settings ===================
APP_VERSION=2.0.0

# =================== Optional Settings ===================
SESSION_STORAGE_PATH=./data/sessions
DOCUMENTS_PATH=./data/documents
LOGS_PATH=./logs
